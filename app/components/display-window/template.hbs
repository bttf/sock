<div class="text-center">
  {{#if isFetching}}
    <h1>
      {{fa-icon 'cog' spin=true}}
    </h1>
  {{/if}}
</div>

<div class='row'>
  <div class="col-xs-2">
    {{#if imgUrl}}
      <img class='img-responsive' src="{{imgUrl}}" alt="sock image preview">
    {{/if}}
  </div>
  <div class="col-xs-10">
    <h2>{{title}}</h2>
    <h4>{{desc}}</h4>
  </div>
</div>
